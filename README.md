Remote Debug plugin for vim
===========================

This plugin builds on the support provided by pyclewn for debugging using gdb
from within vim by adding some wrappers to enable remote debugging using
gdbserver.
